# FourBank
FourBank API é um sistema bancário digital completo desenvolvido em Java 21 e Spring Boot 3. Conta com segurança via OAuth2/JWT, persistência no PostgreSQL via JPA/Hibernate, cache com Redis, mensageria assíncrona usando RabbitMQ e ambiente fully-containerized via Docker Compose para simular transações financeiras em tempo real com alta performance
